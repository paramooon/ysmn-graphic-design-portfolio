const frames=[...document.querySelectorAll('[data-reel-frame]')],counter=document.querySelector('[data-reel-counter]'),bars=[...document.querySelectorAll('.reel-progress i')];let current=0;function showFrame(n){frames.forEach((f,i)=>f.classList.toggle('is-active',i===n));bars.forEach((b,i)=>b.classList.toggle('is-active',i===n));counter.textContent=`${String(n+1).padStart(2,'0')} / 04`}showFrame(0);if(!matchMedia('(prefers-reduced-motion: reduce)').matches)setInterval(()=>{current=(current+1)%frames.length;showFrame(current)},1500);